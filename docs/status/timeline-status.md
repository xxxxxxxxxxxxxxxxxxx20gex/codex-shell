# 任务时间线状态

- 模块职责：把用户消息、智能体消息、计划、工具、命令和错误归一化为结构化多轮时间线。
- 当前状态：消息、计划、推理、命令、文件、MCP、动态工具、搜索、图片与子智能体活动均已进入结构化多轮时间线；长 Session 使用可变高度视口虚拟化，只挂载可见 Turn。
- 最近变更：新增智能跟随滚动，只有用户位于底部附近时才跟随流式输出；查看历史时显示“返回最新/有新内容”，右侧用户消息导航轨可直接跳转到指定 Turn。单 Turn 渲染已从虚拟列表编排中拆出。
- 当前接口：`agentSessionReducer`、`ConversationTimeline`、`ConversationTurn`、`TurnActivityItem`、`TurnPlanView`。
- 已知问题：MCP/动态工具的结构化结果仍以安全截断 JSON 展示；尚未提供活动类型过滤。
- 下一步：增加活动筛选、复制输出和单个超长命令日志虚拟化。
- 验证证据：reducer 测试覆盖命令/Plan delta、MCP progress、Item 生命周期、实时 Diff、历史文件变更重建和有界历史；静态及 happy-dom 测试覆盖 Turn 内容、用户消息跳转、离底后不强制跟随和新内容提示。
- 最后更新：2026-08-11
